//require "test2.tau";
public func printf(x: string, y: int, c: int);

struct boo{
    public x: int;
    public y: int;
}
func foo()-> int {
    x: boo;
    x.x = 1;
    x.y = 2;
    return x.x + x.y;
}

public func main() -> int{
    printf("foo: %i%c", foo(), 10);
    return 0;
}
