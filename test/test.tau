require "foo/bar.tau";
x:=3;
func main(args: String[]){
    extend bar;
    import doo.{foo = (a, b), foo.bar, baz.doo};
    if (x) give 3 else give 5;
    func test_compound_assignment(){
        //x:= 3;
        using ((), a, ((),),) = 17;
        (x, y, (a,b,c), d) := foo();
        (x, y:, (a,b: int,c), d) = foo();
        (a, b) += 3;
        x * #label x match foo{
            while true {*x = 3;} => bar;
            x || Foo(x) => {
                return x;
            }
        };
        
        while true label y loop{
            continue for;
            return 12;
        }
        (x: int) := foo;
        
        x:= do label y while true{
            }
            finally {
                x++;
            } 
            * 3;
           
        using x = if x > 3{
            return 3 * {
            break foo give 12;
            return 12;
            };
        };
        label foo;
    }
    return 0;
}
