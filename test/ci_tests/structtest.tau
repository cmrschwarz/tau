require "support/libc.tau";
import libc::(printf);

struct foo{
    lorem : int;
    ipsum: int;
    struct bar{
        x: int;
        y: string;
    }
}

public func main() -> int{
    f: foo;
    fp := &f;
    (*fp).ipsum = 17;
    printf("ipsum: %i\n", (*fp).ipsum);
    return 0;
}
