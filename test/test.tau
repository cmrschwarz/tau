//require "test2.tau";
module foo{
    module bar{
        public x: int = 5;
    }
    public struct my_struct{
        x: int;
    }
}

func main(z: int) -> int{
    import foo;
    import foo::bar;
    x:= foo::my_struct::x;
    return foo::bar::x;
}