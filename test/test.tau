require "test2.tau";
#x: int;
func foo(){
    match (true) @foo{
        foo => continue @foo;
        bar => continue;
        baz => return 12;
    }
   /* (x: int) := foo;
    x: int = 32;
    y: float = 64;*/
}
module doo{
    import bar;
}
