require "support/libc.tau";
import libc::(printf);
require "bigtest/foo.tau";
require "bigtest/bar.tau";
require "bigtest/baz.tau";
import bar;
import baz;
import foo;


public func main() -> int{
    printf("foo: %i\n", foo::f10000() + bar::f10000() + baz::f10000());
    return 0;
}
