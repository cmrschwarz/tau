import bar;
module bar;
x:=3;
func main(args: String[]){    
    func test_compound_assignment(){
        do label@{
            bar();
        };
        if (x) 3 else 5;
        x := 3;
        using ((), a, ((),),) = 17;
        (x, y, (a,b,c), d) := foo();
        (x, y:, (a,b: int,c), d) = foo();
        (a, b) += 3;
        x * #match(foo) blk@{
            while true {*x = 3;} => bar;
            x || Foo(x) => {
                return x;
            }
        };
        
        while true foo@{
            continue @for;
            continue;
            return 12;
        }
        (x: int) := foo;
        
        y:= do while true {
                foo();
            }
            finally {
                x++;
            } 
            * 3;
        using z = if (x > 3) foo@{
            return 3 * {
            break @foo 12;
            break;
            return 12;
            };
        }
        else{
            break 27;
        };
    }
    return 0;
}
