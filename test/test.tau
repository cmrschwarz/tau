func main(args: String[]){
    if (x) give 3 else give 5;
    func test_compound_assignment(){
        //x:= 3;
        ((), a, ((),),) = 17;
        (x, y, (a,b,c), d) := foo();
        (x, y:, (a,b: int,c), d) = foo();
        (a, b) += 3;
        x * label x match foo{
            while true {*x = 3;} => bar;
            x || Foo(x) => {
                return x;
            }
        };
        
        label y loop{
            return 12;
        }
        
        x:= label y while true{
            }
            finally {
            } 
            * 3;
           
        alias x -> if x > 3{
            return 3 * {
            give 127;
            return 12;
            };
        };
        label foo;
    }
    return 0;
}
