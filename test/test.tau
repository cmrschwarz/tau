//require "test2.tau";

module b{
    public global: int = 30;

    public func foo() -> int{
        x := 10;
        return global * global;
    }
}
module a{
    import b;
    public func printf(x: string, y: int, z: int);
}

import a;
import b;
public func main() -> int{
    a::printf("foo: %i%c", b::global * b::foo(), 10);
    return 0;
}
