//require "test2.tau";


global: int = 30;

func foo() -> int{
    x := 10;
    return global * global;
}

func printf(x: string, y: int, z: int);

public func main() -> int{
    printf("foo: %i%c", global * foo(), 10);
    return 0;
}