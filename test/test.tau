import doo::{boo, (a,b,c)};
a := 3;
func main(z: int) -> int{
    x := 1 + 2;
    y := main(3);
    w:= {
        break 17;
    };
    return x + y + 1 + w;
}

//require "test2.tau";
module doo{
    module boo{
    }
}
